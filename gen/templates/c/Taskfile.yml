version: '3'

tasks:
  default:
    cmds:
      - task --list-all

  build:
    aliases: ["b"]
    cmds:
      - cmake -G Ninja -B build
      - cmake --build build

  run:
    deps: ["build"]
    cmds:
      - ./build/main.exe
      
  clean:
    aliases: ["c"]
    cmds: 
      - 'if {{"{{"}} eq OS "windows" {{"}}"}}; 
        then powershell Remove-Item -Recurse -Force build; 
        else rm -rf build; fi'
